# Generated by IBM TransformationAdvisor
# Wed Feb 27 07:28:32 UTC 2019

apiVersion: v1
kind: Service
metadata:
  name: bookmydoc
  labels:
    app: bookmydoc
spec:
  ports:
    - port: 9080
  selector:
    app: bookmydoc
  type: NodePort

---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: bookmydoc
  name: bookmydoc
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: bookmydoc
    spec:
      containers:
      - image: mycluster.icp:8500/default/bookmydoc:latest
        name: bookmydoc
        env:
          - name: WLP_LOGGING_CONSOLE_FORMAT
            value: json
          - name: WLP_LOGGING_CONSOLE_LOGLEVEL
            value: info
          - name: WLP_LOGGING_CONSOLE_SOURCE
            value: message,trace,accessLog,ffdc
        ports:
        - containerPort: 9080
          protocol: TCP
